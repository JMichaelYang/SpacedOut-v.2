using UnityEngine;
using System.Collections;
using UnityEngine.Networking;

public class NetworkStartup : MonoBehaviour
{
	void Start ()
    {
        //NetworkManager.singleton.GetStartPosition().rotation = new Quaternion(0.5f, 0.5f, -0.5f, -0.5f);
    }
}
